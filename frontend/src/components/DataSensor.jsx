import { Home, Settings, BarChart3, Activity } from "lucide-react";

export default function DataSensor() {
  return (
    <div>
      <h2 className="text-2xl font-semibold mb-4">📈 Data Sensor</h2>
      <p>Menampilkan data suhu, kelembapan, dan aktivitas hama dari sensor IoT.</p>
    </div>
  );
}
