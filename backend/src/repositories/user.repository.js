// Responsibilities: DB access for users (stub)

export const findUserByEmail = async (email) => {
  // TODO: use User model to find by email
  return null;
};

export const createUser = async (userData) => {
  // TODO: persist user
  return null;
};
