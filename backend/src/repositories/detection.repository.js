// Responsibilities: DB access for detections (stub)

export const listDetections = async (query = {}, opts = {}) => {
  // TODO: use Detection model to query
  return [];
};

export const saveDetection = async (payload) => {
  // TODO: persist detection
  return null;
};
